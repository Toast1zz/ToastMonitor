# Build artifacts
.build/
dist/
.buildSystem_*

# User data & credentials — NEVER commit
backups/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.keychain

# macOS
.DS_Store

# Internal working docs — keep local, never publish
DEEPSEEK_AUDIT_REPORT.md
HERMES_AGENT_REMEDIATION_PLAN.md
docs/AUDIT-*.md
